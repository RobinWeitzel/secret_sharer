import '../style.css';

// Initialize page when DOM is ready
document.addEventListener('DOMContentLoaded', initDecryptPage);

function initDecryptPage(): void {
  // Decrypt page logic will be implemented here
  console.log('Decrypt page initialized');
}
